import { Vue, Component, Watch, Prop, Inject, Provide, Model } from 'vue-property-decorator';

@Component
export default class {{pascalCase name}} extends Vue {
    // region Vue Instance lifecycles
    // endregion
    // region computed properties
    // endregion
    // region Async state mutators (async triggers), Watcher Entries
    // endregion
    // region Events triggers
    // endregion
    // region Domain/Feature orientated components
    // endregion
}
